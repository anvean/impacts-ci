{
  "name": "impact-ci",
  "version": "1.0.0",
  "description": "Identify impacted unit and regression tests based on code changes.",
  "main": "dist/index.js",
  "author": "Anvean Technologies",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "bundle": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "package": "npm run build && npm run bundle",
    "all": "npm run package"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}